@model Pokemon
<img src="@Model.Icon" alt="@Model.Name">
<h2>@Model.Name</h2>
<h3>Known Moves</h3>
<ul>
    @foreach (KnownMove move in Model.MovesKnown)
    {
        <li>@move.Move.Name - <span class="fw-lighter fst-italic">@move.Move.MoveType</span></li>
    }
</ul>